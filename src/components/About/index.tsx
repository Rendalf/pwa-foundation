import React from 'react'
import * as styles from './style.css'

const About: React.StatelessComponent = () => {
  return (
    <section className={ styles.About }>
      This is future PWA application
    </section>
  )
}

export default About
