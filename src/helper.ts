import _ from 'lodash'
import React from 'react'
import ReactDOM from 'react-dom'
import { cube, square } from './math'

console.log(React, ReactDOM, cube, square)

console.log(
  _.join([
    'Hello!', 'I am "helper.ts"!',
  ], ' ')
)
